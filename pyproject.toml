[dependency-groups]
dev = [
    "pre-commit==4.6.0",
]

[project]
dependencies = []
dynamic = ["version"]
name = "uv-actions"
requires-python = "==3.14.*"

[tool.uv]
add-bounds = "exact"
exclude-newer = "P7D"
required-version = "~=0.11.0"
