[dependency-groups]
dev = [
    "pre-commit==4.6.2",
]

[project]
dependencies = []
dynamic = ["version"]
name = "uv-actions"
requires-python = "==3.14.*"

[tool.uv]
add-bounds = "exact"
required-version = "~=0.12.0"
