[dependency-groups]
dev = [
    "pre-commit==4.5.1",
]

[project]
dependencies = []
dynamic = ["version"]
name = "uv-actions"
requires-python = "==3.14.*"

[tool.uv]
add-bounds = "exact"
exclude-newer = "P7D"
required-version = ">=0.10, <0.12"
